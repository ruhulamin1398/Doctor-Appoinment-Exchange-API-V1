<!-- header.ejs -->

<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="background-color:#030710 !important">
    <div class="container">
      <a class="navbar-brand" href="#">Chandrabindhu</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <% allWords.forEach(category => { %>
            <li class="nav-item">
              <a class="nav-link" href="#" onclick="loadWords('<%= category.category %>')"><%= category.category %></a>
            </li>
          <% }) %>
        </ul>
      </div>
    </div>
  </nav>
  